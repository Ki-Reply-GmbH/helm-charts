{{- if and .Values.rbac.enable .Values.leaderElection.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "openbao-operator.fullname" . }}-leader-election-rolebinding
  labels:
    {{- include "openbao-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "openbao-operator.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "openbao-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
