apiVersion: v1
kind: Service
metadata:
  name: {{ include "openbao-ui.fullname" . }}
  labels:
    {{- include "openbao-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "openbao-ui.selectorLabels" . | nindent 4 }}
