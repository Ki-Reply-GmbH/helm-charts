apiVersion: delivery.ocm.software/v1alpha1
kind: Component
metadata:
  name: platform-mesh
spec:
  repositoryRef:
    name: platform-mesh
  component: github.com/platform-mesh/platform-mesh
  # potentially outdated
  semver: 0.3.0-build.1095
  # new 
  # semver: 0.4.0-build.518
  interval: 1m
  ocmConfig:
    - kind: Repository
      apiVersion: delivery.ocm.software/v1alpha1
      name: platform-mesh
      namespace: default
